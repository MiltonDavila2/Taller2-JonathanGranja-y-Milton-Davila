<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.NewPage2"
             Title="NewPage2">
    <ScrollView>
        <StackLayout Margin="20">
            <Label Text="Texto 1" FontSize="Header" HorizontalOptions="Center" VerticalOptions="Center" />
            <Label Text="Texto 2" FontSize="Medium" HorizontalOptions="Center" VerticalOptions="Center"/>
            <Label Text="Texto 3" FontSize="Micro"  HorizontalOptions="Center" VerticalOptions="Center"/>
            <Button x:Name="CounterBtn" Text="FlexLayout" SemanticProperties.Description="Hola" Clicked="OnCounterClickedAstackLayout" />

        </StackLayout>
    </ScrollView>
</ContentPage>